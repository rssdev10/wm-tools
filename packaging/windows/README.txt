Windows packaging notes

- Archives are produced as zip files containing the `.exe` and supporting files.
- Extract the full archive before launching the application.
- You may need to install USB serial drivers for the oscilloscope connection.
