Linux packaging notes

- Archives are produced as zip files for parity with the other operating systems.
- The executable can be run directly after extracting the archive.
- Ensure your user has permission to access USB serial ports (e.g. in the `dialout` group).
